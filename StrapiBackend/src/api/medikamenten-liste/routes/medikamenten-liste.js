'use strict';

/**
 * medikamenten-liste router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::medikamenten-liste.medikamenten-liste');
