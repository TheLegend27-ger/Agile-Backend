'use strict';

/**
 * medikamenten-liste service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::medikamenten-liste.medikamenten-liste');
